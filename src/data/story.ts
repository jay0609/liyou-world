/**
 * 由 scripts/convert-content.cjs 自动生成
 * 编辑请用 Decap CMS：cs2npc.cn/admin
 */

export interface StoryLine {
  speaker: string
  text: string
  options?: { label: string; next: number }[]
}

export const storyLines: StoryLine[] = [
  {
    "speaker": "旁白",
    "text": "在很久很久以前——或者说，在所有时间开始之前——有一位孤独的写书人。"
  }
]
